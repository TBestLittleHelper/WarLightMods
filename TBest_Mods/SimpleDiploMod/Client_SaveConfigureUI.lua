function Client_SaveConfigureUI(alert)
    Mod.Settings.NumTurns = numberInputField.GetValue(); --turns the mod is active. After it is an FFA.
    Mod.Settings.TimeToWar = timeToWarInputField.GetValue(); --turns to declere war. Typicaly 2.
end
