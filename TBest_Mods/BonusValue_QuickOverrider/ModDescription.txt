Each bonus is by default set according to the following formula.

BonusValue = numTerriotries + N

There is alos an optional X factor, that can randomely change EACH bonus by anything from -X to +X.
